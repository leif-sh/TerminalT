# 阶段 3 自动化验收记录

> 执行日期：2026-08-12
> 环境：Windows、Rust loopback TCP、仓库内 `russh` SSH 测试服务器

## 自动化结果

| 验收项 | 覆盖方式 | 结果 |
| --- | --- | --- |
| 10 个并存 SSH 会话 | 同一 loopback SSH 服务保持 10 个已认证连接；额外注入一次错误密码认证 | 通过 |
| 单会话异常隔离 | 错误认证后，逐一为原 10 个连接创建 PTY/Shell，并以唯一探针验证双向数据 | 通过 |
| 5 MiB 持续输出 | 服务端以 64 KiB 数据块主动发送 5 MiB；客户端校验字节数与内容 | 通过 |
| 高频输出后的交互 | 5 MiB 接收完成后立即发送 `still-interactive` 并校验回显 | 通过 |
| UTF-8 与控制序列 | 校验中文、Emoji、全角宽字符、组合字符、真彩色及清屏/定位序列逐字节无损 | 通过 |
| 会话注册与释放 | 重复会话 ID 阻止、单会话关闭、全部会话关闭、2 秒有界退出 | 通过 |
| 重连状态隔离 | 前端 reducer 验证原标签 ID、重连来源和其他标签状态不变 | 通过 |
| 临时凭据重连 | 前端领域测试验证密码和私钥口令不会进入重连状态 | 通过 |
| 显示与 keepalive 设置 | 范围归一化测试、Rust 参数校验及本地浏览器控件联动检查 | 通过 |

执行命令：

```powershell
npm run release:version
npm run release:test
```

## 仍需人工真实服务器验收

本机 `sshd` 服务存在但未启动。本轮未修改系统服务状态，也未使用真实账号或凭据，因此下列项目保持人工待验收：

- 使用 `vim`、`top`、`less`、`tmux` 验证全屏绘制、功能键、鼠标模式和窗口 resize；
- 使用 Windows 中文输入法验证组合输入期间不提前发送、不截断、不重复；
- 鼠标拖拽选择、系统剪贴板权限及右键复制粘贴的桌面端体验；
- 真实网络中断、服务器重启和 8 小时长稳运行。

在这些人工项完成前，阶段状态保持“联调中”，不宣称达到阶段退出条件。
